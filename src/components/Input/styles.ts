import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    height: 48,
    width: "100%",
    borderWidth: 1,
    borderRadius: 8,
    borderColor: "#c3c5cb",
    paddingHorizontal: 16,
  },
});
